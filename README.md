For CIRCOM circuit compilations & generating files run commands

./compile.sh

./generateWitness.sh

download tau file

./executeGroth16.sh

npm install -g snarkjs@latest
(to make sure global & local snarkjs are latest..)
